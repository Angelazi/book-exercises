first_ten <- 10:20
next_ten <- seq(21:30)
next_ten <- seq(21,30)
all_numbers < c(first_ten + next_ten)
all_numbers <- c(first_ten + next_ten)
all_numbers <- first_ten + next_ten
first_ten <- c(10:20)
next_ten <- seq(21,30)
# Create a vector `all_numbers` by combining the previous two vectors
all_numbers <- first_ten + next_ten
eleventh <- all_numbers[11]
eleventh <- all_numbers[11]
print(eleventh)
print(all_numbers)
all_numbers <- first_ten + next_ten
some_numbers <- all_numbers[2:5]
even <- 1:100:2
even <- seq(2, 100, 2)
?ALL
?all
filter <- all(even %% 2 == 0)
phone_numbers <- c(8, 6, 7, 5, 3, 0, 9)
prefix <- phone_numbers[1:3]
# Create a vector `small` that has the values of `phone_numbers` that are
# less than or equal to 5
small <- phone_numbers < 5
small <- phone_numbers < 5
result <- phone_numbers[small]
large <- phone_numbers > 5
result2 <- phone_numbers[large]
new_phone <- gsub(result2, 5, phone_numbers)
place the values in `phone_numbers` that are larger than 5 with the number 5
new_phone <- gsub(phone_numbers > 5, 5, phone_numbers)
# Replace every odd-numbered value in `phone_numbers` with the number 0
new_phone2 <- gsub(phone_numbers %% 2 != 0, 0, phone_numbers)
